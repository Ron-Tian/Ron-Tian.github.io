{
  "name": "stock-watchlist-sync",
  "version": "1.0.0",
  "description": "Cloudflare Worker for syncing stock watchlist across devices",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
